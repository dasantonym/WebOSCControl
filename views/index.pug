doctype html
html
    head
        title Web OSC Control
        meta(name="fragment" content="!")
        base(href='/')
        link(rel='stylesheet', href='/css/style.css')
body
    nav.navbar.navbar-default
        .container-fluid
            .navbar-header
                button.navbar-toggle.collapsed(type='button', data-toggle='collapse', data-target='#woc-navbar-collapse', aria-expanded='false')
                    span.sr-only Toggle navigation
                    span.icon-bar
                    span.icon-bar
                    span.icon-bar
                a.navbar-brand(href='/') Web OSC Control
            #woc-navbar-collapse.collapse.navbar-collapse
                ul.nav.navbar-nav
                    li.active
                        a(href='/presets') Presets
    .container-fluid(ng-view)

    scripts
        script(src='/bower_components/osc.js/dist/osc-browser.min.js')
        script(data-main="js/rjs-runtime" src='/bower_components/requirejs/require.js')